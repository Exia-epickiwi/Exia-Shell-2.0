#include <stdio.h>
#include <stdlib>
int ContainNumber (char**words)
while( i<1; next ;i++) {
cout << "nombre:";
while( !(cin >> ContainNumber) ) {
    string tmp;
    cin.clear();
    cin >> tmp;
    cout << "incorrect" << endl;
return NULL;
return words [I];
}
}
