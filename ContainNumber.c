    #include <stdio.h>
    #include <stdlib.h>
    int main(void)
    {
      
        int ContainNumber (char**pend);
        int i
        char *pend = NULL;
        for( i<1; next != NULL ;i++) {
        do
        {
            printf("Tappez un numerateur: ");
            fflush(stdout);
            fgets(tampon, sizeof tampon, stdin);
            fclean(tampon, stdin);
            nombre = strtol(tampon, &pend, 0);
        }
        while (*pend != 0);
        return NULL;
    }
  }
